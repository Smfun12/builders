To run the build of project, execute project-build.sh script
For build project only with:
	maven:
		1. run in console 'mvn install'
	gradle:
		1. run in console 'gradle build' and then 'gradle publishToMavenLocal'